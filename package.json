{
  "name": "flux-datastore",
  "version": "0.0.1",
  "description": "flux datastore",
  "main": "lib/DataStore.js",
  "dependencies": {
    "flux": "^2.1.1",
    "immutable": "^3.7.4",
    "object-assign": "^4.0.1",
    "lodash.template": "^3.6.2",
    "lodash.debounce": "^3.1.1",
    "jquery": "git+https://github.com/kairosagency/jquery.git#ajax",
    "operative": "^0.4"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.1.0",
    "babelify": "^7.0.0",
    "browserify": "^12.0.0",
    "envify": "^3.4.0",
    "git-hooks": "^1.0.2",
    "qunitjs": "^1.19.0",
    "uglify-js": "^2.4.0",
    "watchify": "^3.2.0",
    "benchmark": "^1.0.0",
    "faker": "*"
  },
  "scripts": {
    "start": "watchify tests/tests.js -o tests/bundle-tests.js -v -d --poll",
    "start-benchmark": "watchify tests/benchmark.js -o tests/bundle-benchmark.js -v -d --poll",
    "build": "babel -d lib src --presets es2015",
    "build-test": "browserify tests/tests.js > tests/bundle-tests.js",
    "build-benchmark": "browserify tests/benchmark.js > tests/bundle-benchmark.js"
  },
  "author": [
    "Alexandre Assouad",
    "Matthieu Faule"
  ],
  "browserify": {
    "transform": [
      "babelify"
    ]
  }
}
